<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>TODO</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <%= render 'shared/navbar' %>
    <%= javascript_include_tag 'application' %>
    <div class="layout-tour">
      <div class="layout-tour-navigator">
        <img src="<%= image_path("happy.svg") %>" class="navigator-mascot" alt="">
        <div class="layout-tour-menu">
          <h3 class="layout-tour-menu-title">Data Tour</h3>
          <ol class="layout-tour-menu-items">
            <li><%= link_to('Projects', projects_path) %></li>
            <li><%= link_to('Reviews', project_reviews_for_entity_path(params[:project_id])) %></li>
            <li><%= link_to('Review traffic', project_reviews_path(params[:project_id])) %></li>
            <li><%= link_to('Common words', project_entities_data_path) %></li>
            <li><%= link_to('Keyword insights', project_entities_path) %></li>
            <li><%= link_to('Conclusion', project_conclusion_path(params[:project_id])) %></li>

          </ol>
        </div>
      </div>
      <div class="layout-tour-content">
    <h1 class="layout-tour-title">Keyword insights</h1>

        <div class="layout-tour-content-inner">
              <%= yield %>

        </div>
      </div>
    </div>
    <%= javascript_pack_tag 'application' %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
  <script>
    function toggleNav() {
      document.getElementById("mySidebar").classList.toggle("sidebar-closed")
      document.querySelector(".main").classList.toggle("sidebar-closed");
      document.getElementById("textu-logo").classList.toggle("sidebar-closed");
    }
</script>
</html>
